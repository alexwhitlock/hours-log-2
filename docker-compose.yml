services:
  hours_log:
    build: .
    container_name: hours_log
    ports:
      - "127.0.0.1:5005:5000"
    volumes:
      - ./config.json:/app/config.json:ro
      - ./data:/app/data
      - ./logs:/app/logs
    environment:
      - DB_PATH=/app/data/hours_log.db
      - LOG_DIR=/app/logs
    restart: unless-stopped
